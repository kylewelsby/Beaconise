Feature: Manage venue_syncs
  In order to Sync Venue with External sources
  As an Business
  wants to search and retrieve venue ID
  
  Scenario: Sync Venue
    Given I have a venue named Matter
    And I press "Sync with TillLate"
	Then I should see the 
  Scenario: Delete venue_sync
    Given the following venue_syncs:
      ||
      ||
      ||
      ||
      ||
    When I delete the 3rd venue_sync
    Then I should see the following venue_syncs:
      ||
      ||
      ||
      ||
