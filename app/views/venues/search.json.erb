{
	query:"<%= @q %>",
	suggestions:[
	<%- @venues.each do |v| -%>
		"<%=h v.name %> - <%=h v.city %>",
	<%- end -%>
	],
	data:[
	<%- @venues.each do |v| -%>
		<%- v[:html] = render(:partial => "venues/venue.html", :object => v) -%>
		<%= v.to_json %>,
	<%- end -%>
	]
}