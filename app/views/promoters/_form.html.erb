<% semantic_form_for @promoter, :html => {:multipart => true} do |f| %>
	<%= f.error_messages %>
	<% f.inputs :name => "Details" do %>
		<%= f.input :name %>
		<%= f.input :email %>
		<%= f.input :telephone %>
	<% end %>
	<%= f.buttons %>
<% end %>