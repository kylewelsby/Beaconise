defaults: &defaults
    # Required.
    api_key: e8ac4c4b1065e7e29df9597df6bafcae
    secret_key: e5fc329e296dd138df42c954e319ca7b

    # Optional for Facebook Connect.
    # canvas_page_name: YOUR_APP_CANVAS_NAME
    # Optional for Facebook Connect. Depends on your Facebook App settings.
    # callback_url: http://localhost:3000

    # Optional options.
    # pretty_errors: true
    # set_asset_host_to_callback_url: false
    # tunnel:
    #   public_host_username:
    #   public_host:
    #   public_port: 4007
    #   local_port: 3000

development:
  <<: *defaults

test: &test
  <<: *defaults

production: &production
  <<: *defaults

# staging:
#   <<: *production
#
# cucumber:
#   <<: *test
